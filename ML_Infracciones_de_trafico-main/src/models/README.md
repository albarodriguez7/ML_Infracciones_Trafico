# models

Modelos entrenados guardados en formato **pickle** o **joblib**.

- El notebook principal `main.ipynb` debe incluir como paso final el guardado del modelo (o modelos) en esta carpeta.
- Ejemplo: `joblib.dump(modelo_final, "src/models/modelo_final.joblib")`.
